{
  "name": "album-ai-backend",
  "private": true,
  "version": "1.0.0",
  "description": "Vercel serverless backend for the portfolio album AI (Q&A, captions, RAG).",
  "type": "module",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "dev": "vercel dev",
    "start": "vercel dev",
    "build": "echo \"No build step for serverless functions\"",
    "check": "node --check api/ai.js && node --check api/ai-expert.js && node --check api/ask.js && node --check api/delete-doc.js && node --check api/docs.json.js && node --check api/img.js && node --check api/ingest-pdf.js && node --check api/ping.js && node --check api/skills.js && node --check db.js"
  },
  "dependencies": {
    "@ai-sdk/deepinfra": "^0.1.0",
    "ai": "^4.0.0",
    "pdf-parse": "^1.1.1",
    "pg": "^8.11.5",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "vercel": "^33.7.0"
  }
}
